# Copy to .env and fill in. .env is gitignored — secrets never get committed.

# --- Plex (server-side only) ---
# Base URL of your Plex server, e.g. http://192.168.1.10:32400
PLEX_URL=
# Plex auth token. How to find it:
# https://support.plex.tv/articles/204059436-finding-an-authentication-token-x-plex-token/
PLEX_TOKEN=

# --- Lidarr (server-side only) ---
# Base URL of Lidarr, e.g. http://192.168.1.10:8686
LIDARR_URL=
# Lidarr API key: Settings > General > Security > API Key
LIDARR_API_KEY=

# --- LLM provider (Sonic Sage) ---
# Active provider: claude | openai | ollama
LLM_PROVIDER=claude
# Used only by the matching provider:
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
OLLAMA_URL=http://localhost:11434

# --- App ---
# Port the server listens on
PORT=8080
# Require login with Plex (anyone with access to your Plex server). Recommended
# if you expose Resonarr beyond your LAN. Set to true to enable; unset to open.
# Always pair with HTTPS (a reverse proxy / tunnel) so the session cookie and
# Plex tokens aren't sent in the clear. Lockout escape hatch: unset and restart.
AUTH_PLEX=
# Optional HTTP Basic auth — set BOTH to require a single shared login. An
# alternative to AUTH_PLEX; use one or the other, not both.
AUTH_USER=
AUTH_PASS=
# Where the SQLite DB + settings live. Defaults to /config in the container,
# or <repo>/.data in local dev. Override here if you want a custom path.
# DATA_DIR=/config
