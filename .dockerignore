node_modules
**/node_modules
# .git is intentionally NOT ignored: the build reads the commit SHA from it for
# the version stamp, then deletes it before the runtime image is assembled.
.env
.env.local
**/dist
*.log

# Runtime data (host-mounted) — anchored to root so it does NOT match
# source dirs like server/src/config.
/config
/*.sqlite
